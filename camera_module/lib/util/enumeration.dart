enum CameraType { RECTANGLE, CIRCLE, SIGNATURE, NONE }

enum CameraResolution { LOW, MEDIUM, HIGH, VERY_HEIGH, ULTA_HEIGH }

enum CameraDirection { BACK, FRONT, ONLY_FRONT, ONLY_BACK }

enum PicType { FILE, URL }
